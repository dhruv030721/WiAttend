import 'package:flutter/material.dart';

class DownloadExcelScreen extends StatelessWidget {
  const DownloadExcelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
